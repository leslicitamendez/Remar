<%= form_for(@city) do |f| %>
  <% if @city.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@city.errors.count, "error") %> prohibited this city from being saved:</h2>

      <ul>
      <% @city.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre, 'Nombre' %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :estado, 'Provincia o Pais' %><br>
    <%= f.text_field :estado %>
  </div>
  <div class="actions">

    <%= f.submit (@city.new_record? ? 'Registrar Ciudad' : 'Editar Ciudad') , :class => "btn btn-mini btn-success" %>
  </div>
<% end %>
